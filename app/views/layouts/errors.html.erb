<!DOCTYPE html>
<html>
<head>
  <title>Dangerzone!</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <style>
    @font-face {
      font-family: 'bettynoir';
      src: url("/bettynoir.ttf") format('truetype');
    }
    .end_banner {
      // margin-top: 70px;
      position: absolute;
      height: 100vh;
      width: 100vw;
    }

    #myendVideo {
      // position: fixed;
      right: 0px;
      bottom: 0px;
      min-width: 100%;
      min-height: 100vh;
    }

    .end-content {
      font-family: 'bettynoir';
      position: absolute;
      bottom: 50%;
      left: 50%;
      background: rgba(0, 0, 0, 0);
      color: $black;
      padding: 0px;
      margin-left: -250px;
      width: 500px;
      text-align: center;
      margin-bottom: -45px;
      font-size: 40px;
    }

    .end-content-h1 {
      color: $black;
      text-shadow: 2px 3px 15px rgba(235, 235, 235, 0.4);
      &:hover {
        color: $black;

      }
    }
  </style>
</head>
<body>
  <%= yield %>
</body>
</html>
